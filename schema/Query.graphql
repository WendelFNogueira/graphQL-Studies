# Pontos de entrada da Api
type Query {

    usuarioLogado: Usuario

    listarProdutos: [Produto]

    produtoEmDestaque(id: Int): Produto

    listarUsuarios: [Usuario]

    usuario(id: Int): Usuario

    perfis: [Perfil]

    perfil(id: Int): Perfil

}